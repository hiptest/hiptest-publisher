# encoding: UTF-8
import unittest
from actionwords import Actionwords

class Test{{{ camelize node.parent.children.name }}}(unittest.TestCase):
    def setUp(self):
        self.actionwords = Actionwords(self)
{{#indent}}
{{#each rendered_children.scenarios}}{{{this}}}
{{/each}}
{{/indent}}
