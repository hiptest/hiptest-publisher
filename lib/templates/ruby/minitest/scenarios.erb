# encoding: UTF-8

require 'minitest/autorun'
require_relative 'actionwords'

class Test<%= parent.children[:name].camelize %> < MiniTest::Unit::TestCase
  include Actionwords

<%= indent_block(@rendered_children[:scenarios], nil, "\n") %>
end