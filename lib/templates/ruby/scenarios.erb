# encoding: UTF-8
require 'spec_helper'
require_relative 'actionwords'

describe '<%= parent.children[:name].camelize %>' do
  include Actionwords

<%= indent_block(@rendered_children[:scenarios], nil, "\n") %>
end