# encoding: UTF-8
require 'spec_helper'
require_relative 'actionwords'

describe '{{{ camelize node.parent.children.name }}}' do
  include Actionwords
{{#indent}}
{{#each rendered_children.scenarios}}{{{this}}}

{{/each}}
{{/indent}}
end