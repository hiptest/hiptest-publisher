"<%= node.children[:chunks].map do |chunk|
    if chunk.is_a? Zest::Nodes::Variable
      "\#{#{chunk.children[:name]}}"
    else
      "#{chunk.children[:value]}".gsub(/"/) {|_| '\\"' }
    end
  end.join %>"