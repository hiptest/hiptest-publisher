# encoding: UTF-8
import unittest
from actionwords import Actionwords

class Test<%= parent.childs[:name].camelize %>(unittest.TestCase):
    def setUp(self):
        self.actionwords = Actionwords()

<%= indent_block(@rendered_childs[:scenarios]) %>
