# encoding: UTF-8

require 'minitest/autorun'
require_relative 'actionwords'

class Test<%= parent.childs[:name].camelize %> < MiniTest::Unit::TestCase
  def setup
    @<%= context[:call_prefix]%> = Actionwords.new
  end

<%= indent_block(@rendered_childs[:scenarios]) %>
end