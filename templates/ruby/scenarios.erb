# encoding: UTF-8
require_relative 'actionwords'

describe '<%= parent.rendered_childs[:name] %>' do
  before (:all) do
    @<%= @rendered_childs[:call_prefix]%> = Actionwords.new
  end

<%= indent_block(@rendered_childs[:scenarios]) %>
end